set tabstop=2 shiftwidth=2 expandtab
set nocompatible
set ignorecase
set smartcase
set autoindent
set number
set nornu

if has('filetype')
  filetype indent plugin on
endif

" Enable syntax highlighting
if has('syntax')
  syntax on
endif